import { ReactElement } from "react";

const Loading = (): ReactElement => {
  return (
    <div className="w-full h-calcScreen text-2xl flex items-center justify-center">
      Loading...
    </div>
  );
};

export default Loading;
